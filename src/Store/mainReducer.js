const initialState = {
  umpaLumpas: [],
  selectedUmpaLumpaId: 0,
  currentPage: 0,
};

const mainReducer = (state = initialState, action) => {
  switch (action) {
    default:
      return state;
  }
};

export default mainReducer;
